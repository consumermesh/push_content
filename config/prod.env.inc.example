<?php

/**
 * @file
 * Production environment configuration.
 */

$org = 'acme';
$name = 'production';

// Optional: Define bucket for AWS S3 deployments
// $bucket = 'prod-bucket';

// Optional: Define custom commands for different CDN providers
// The actual command construction is handled by the module based on command key
// You only need to define the label and description
$custom_commands = [
  'cloudflare' => [
    'label' => 'Push to Cloudflare (Production)',
    'description' => 'Deploy to Cloudflare CDN (Production)',
  ],
  'bunny' => [
    'label' => 'Push to Bunny CDN (Production)',
    'description' => 'Deploy to Bunny CDN (Production)',
  ],
  'aws' => [
    'label' => 'Push to AWS S3 (Production)',
    'description' => 'Deploy to AWS S3 (Production)',
  ],
  // 'default' is automatically provided if no custom commands are defined
];