<?php

/**
 * @file
 * Production environment configuration.
 */

$org = 'acme';
$name = 'production';

// Optional: Define custom commands for different CDN providers
$custom_commands = [
  'cloudflare' => [
    'label' => 'Push to Cloudflare (Production)',
    'command' => '/opt/cmesh/scripts/deploy-cloudflare.sh -o ' . escapeshellarg($org) . ' -n ' . escapeshellarg($name) . ' --zone-id prod-zone --api-token $CLOUDFLARE_API_TOKEN',
    'description' => 'Deploy to Cloudflare CDN (Production)',
  ],
  'bunny' => [
    'label' => 'Push to Bunny CDN (Production)',
    'command' => '/opt/cmesh/scripts/deploy-bunny.sh -o ' . escapeshellarg($org) . ' -n ' . escapeshellarg($name) . ' --storage-zone prod-storage --access-key $BUNNY_ACCESS_KEY',
    'description' => 'Deploy to Bunny CDN (Production)',
  ],
  'aws' => [
    'label' => 'Push to AWS S3 (Production)',
    'command' => '/opt/cmesh/scripts/deploy-aws.sh -o ' . escapeshellarg($org) . ' -n ' . escapeshellarg($name) . ' --bucket prod-bucket --region us-east-1',
    'description' => 'Deploy to AWS S3 (Production)',
  ],
  'default' => [
    'label' => 'Push to Production (Default)',
    'command' => '/opt/cmesh/scripts/pushfin.sh -o ' . escapeshellarg($org) . ' -n ' . escapeshellarg($name),
    'description' => 'Default push to production environment',
  ],
];